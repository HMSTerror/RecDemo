# 当更强的文本证据反而有害:离散偏好扩散核的 Fallback-Safe 条件化(中文对照版 v2)

> 本文件是 `paper/main_v2.tex`(2026-07-06 重定位新稿)的中文对照,供作者审阅;英文版为投稿母本。红色占位符以 **[待填:...]** 标注,回填责任行:CLOSE-01/02/04/05/07。声明层执行 Family D 冻结纪律(`docs/reports/2026-07-04-family-d-claim-freeze-cn.md`),Gate-1 红灯后果已如实写入。

## 摘要

PreferGrow 一类离散偏好扩散推荐器把用户偏好物品向一个**proposal 分布**褪色腐蚀,再学习把偏好长回来。该家族的个性化完全作用在 **score 侧**——转移核对所有用户共享。我们对核做个性化,并由此揭示一个反直觉事实:**因为 proposal 是腐蚀分布,文本证据可能"好到不能用"**。在四个基准上,一个 train-only 的"文本相似度能否预测下一物品"统计量,与文本倾斜核的结果呈 4/4 完美逆序:文本定义了信息性 hard negative 的地方倾斜有益;文本能预测未来正例的地方倾斜失败——腐蚀变成 false-negative 污染。我们用一个两因子、history-only 的门控实现这一认识:经长度匹配随机历史零点校准的用户连贯性信号,乘以冻结的数据集级效用因子,在宿主可学习 proposal 与冻结文本锚点之间插值。设计是 **fallback-safe** 的:条件化链保持宿主核的马氏性、细致平衡与解析反演;门关闭时**精确**还原宿主;任意门值下与宿主的总变差距离不超过门值。对照组(可靠性重排/全局 proposal/无门锚点)表明机制需要**对齐的**用户级信号;腐蚀诊断显示门随证据退化自适应回退。预注册的四数据集冻结门验证结果混合——开门数据集保持小幅正增益、一个关门数据集保持平价,另两个数据集以与 run-to-run 噪声量级相当的差距未达平价带,我们将其作为**待查的实现问题**报告,而非任一方向的证据。跨数据集逆序仅有四个点;收益按设计是选择性的——文本效用高的地方,方法的价值是安全性而非提升。

## 1. 引言

(前三段:扩散推荐器背景、proposal 的核心地位、"score 侧已被 PreferGrow 个性化,我们问互补问题:核能否被安全地个性化"——与旧稿一致。)

**[新增定位段]** 这个问题正在变得紧迫。一条快速推进的工作线正在让离散扩散的腐蚀过程**自适应化**以服务生成式推荐:掩码扩散下的语义 ID 并行解码(LLaDA-Rec)、置信度排序的任意顺序解码(DiffGRM)、以及由"均匀掩码有害"这一观察驱动的位置自适应掩码调度(arXiv:2601.19501),分析工作也开始跟进(arXiv:2511.23021),底层是离散扩散语言模型纲领(LLaDA/SEDD)。这一浪潮所自适应的对象主要是**模型内生信号**(置信度、位置启发式);它缺少的是**证据定价**——一个训练前可算、决定外部证据是否应被允许塑造腐蚀过程的统计量——以及塑造后过程的安全保证。本文是对该问题的一次**预注册可靠性研究**,选在能把问题提得最干净的最简核家族上。

(中心发现段:直觉是"证据越好越该用",**我们的中心实证发现是这个直觉是反的**——U_ds 与倾斜结果 4/4 逆序;方法段:两因子门 g = g_max·φ(U_ds)·clip(ũ,0,1);理论故意初等但换来 score/loss 侧无法表达的性质:证据无信息**或误导**时可证明地还原宿主——与旧稿一致。)

**四条贡献**(冻结措辞):①效用逆序发现(4/4,p=1/24)+机制解释+U_ds 作为任何"从内容相似度挖负例"方案的 train-only 预检统计量;②带 fallback 安全性的核侧个性化(命题 1 有效性/命题 2 精确归约/引理 1 TV 界);③校准方法论(长度匹配随机历史零点;连贯性即使校准后也错测效用,故数据集因子直接从效用估计并在重训前冻结);④机制证据(对照组+腐蚀回退诊断)。

## 2. 预备:离散偏好扩散

词表 Ω(M+1 态含伪物品 ⋆),行向量约定。秩一核 Π_p(i,j)=p(j),生成元 Q_p=Π_p−I,前向转移 P_{σ,p}=e^{σQ_p}=αI+(1−α)Π_p(α=e^{−σ}):偏好以概率 α 存活,否则从 p 重采样——**偏好向 proposal 褪色**。score 模型以去噪 score entropy(SEDD)训练;反演用解析平稳反演比率,proposal 以闭式出现。宿主的 proposal 是可学习的:p_core=softmax(p1)。以下只改 proposal,其余全部固定。

## 3. 方法:Fallback-Safe 门控 Proposal

**3.1 证据锚点**:每数据集一个冻结文本 bank(题名/品牌/类目/描述),现成编码器一次性嵌入、不微调。历史均值向量 ē_h 定义内容锚点 c_h(i) ∝ exp(⟨ē_h, e_i⟩/τ0),缺文本物品降权。锚点提出**内容上说得通的替代物**,从不替换宿主的 score 表征。

**3.2 用户因子:零点校准连贯性**:原始连贯性 A_h 有高且数据集相关的地板(生产 bank 上随机伪历史已得 0.931-0.942)。对每个数据集和历史长度 L 用冻结 bank 抽随机伪历史,记录 μ_null(L)、σ_null(L),定义 ũ=(A_h−μ_null(L))/(k·σ_null(L))。ũ 是 history-only 且沿轨迹固定。**零点校准必要但不充分**:校准后的连贯性度量"行为是否在文本空间聚簇",不度量"把腐蚀倾向该簇是否有益"——生产 bank 上中位 ũ 排序 ML1M(1.43)>Beauty(0.80)>ATG(0.72)>Steam(0.11),与倾斜真正有益的地方**几乎相反**。这正是需要第二个效用导向因子的原因。

**3.3 数据集因子:冻结文本效用**:直接在训练 split 上测门控真正需要知道的东西——文本相似度是否预测**正例**?U_ds = 锚点相似度把真实下一物品排在流行度采样负例之上的概率(AUC;估计量冻结于附录)。φ(U_ds)=clip((0.70−U_ds)/0.10, 0, 1):U≤0.60 全开(文本定义 hard negative 而不预测正例),U≥0.70 全关(倾斜会把腐蚀质量砸向可能的正例)。**阈值在生产统计量算出之前冻结,事后未调**。

**3.4 门控核**:p_u = p_core(⋆)e_⋆ + (1−p_core(⋆))[(1−g)·renorm(p_core) + g·c_h],g=g_max·φ(U_ds)·clip(ũ,0,1)。三个刻意限制:①伪物品质量精确取宿主值,门不碰;②g 是唯一旋钮——无证据依赖的温度或流行度混合;③无新可学习模块。p_u 替换**每一个**核级对象里的全局 proposal:前向腐蚀、score-entropy 权重、解析反演比率——不只是负采样。

**3.5 理论**(证明在附录;固定 h ⇒ ũ、U_ds、p_u 沿轨迹固定):
- **命题 1(核有效性)**:前向腐蚀闭式 q_t(x|x0,u)=ᾱ_t·1{x=x0}+(1−ᾱ_t)p_u(x);条件马氏;平稳/极限分布为 p_u;对 p_u 细致平衡;解析反演=宿主公式换 proposal 项。
- **命题 2(精确归约/fallback 安全)**:g=0 ⇒ p_u=p_core ⇒ 一切导出对象与宿主核**逐一相等**。
- **引理 1(有界偏离)**:TV(p_u,p_core)≤g;前向转移每行 TV ≤ (1−α_σ)g。
- **注记(为什么核是正确的注入位置)**:encoder/loss 级注入不存在命题 2 的类比——"零可靠性下带文本特征的 encoder"没有"就是宿主模型"的意义。归约保证只在条件化进入转移律本身时可表达。注入位置对比因此是 regime 分析,不是安全主张的依据。

## 4. 效用逆序分析(论文中心展品)

**表 1(冻结数字)**:

| 数据集 | U_ds | φ(U_ds) | Δ NDCG@10(无 φ,第一代) |
|---|---|---|---|
| Steam | 0.570 | 1.000 | +0.0064 |
| ATG | 0.688 | 0.117 | +0.0019 |
| Beauty | 0.712 | 0.000 | −0.0006 |
| ML1M | 0.754 | 0.000 | −0.0622 |

三个读法:①排序完美倒置(唯一明确增益在**最低**效用处,严重损失在**最高**效用处;四点 p=1/24,提示性而非结论性,但 train-only、预注册进 φ、机制上可预期);②逆序对连贯性不可见(ML1M 用户相对其零点**最**连贯,中位 ũ=1.43——连贯性门救不了它,φ 必须来自效用);③流行度解释不了信号(uniform 负例变体给出相同排序 0.799/0.692/0.717/0.565)。

**命题 3(目标碰撞随锚点效用增长)**:前向腐蚀把褪色偏好重定位到真实下一物品 y 的概率 = (1−ᾱ_t)p_u(y),对 g 仿射且当 c_h(y)>p̃_core(y) 时严格递增;对用户平均后斜率为 E_h[c_h(y_h)−p̃_core(y_h)]——U_ds 的质量形式类比。碰撞质量是通道而非终指标伤害的证明(score 模型可部分补偿);表 1 是经验仲裁。

**铰链稳健性**:92 网格点扫描中 65% 保持两条冻结定性结论;预注册点 (0.70,0.10) 落在平台内,吸纳全部第一代增益、零第一代伤害。留一法逆序 4/4 成立。

**用户级读出(诚实负结果)**:用户级逆序**不复现**——ML1M ρ=0.028(n=4832)、Steam ρ=0.046(n=31836),弱且非负;逆序主张保持在数据集级,不升级为用户级规律。

**腐蚀 bank 门响应(观察性)**:冻结 Beauty token-dropout bank 上,U_ds 随 dropout 单调下降(0.7133→0.7044)但全程高于 0.70 关断线,φ 恒为 0;相对干净 bank(0.7124)最轻腐蚀点略高(+0.0008)——仅记录为观察证据,不构成单调律,不支持任何冻结参数更新。

**纯 out-of-sample 检验(诚实 MISS)**:ASO 在四点表**之后**冻结、按预注册规则评估:U_ds=0.538、φ=1.0,冻结预测 Δ>0;实测 **−0.0108,预测未中**。如实记录为 out-of-sample miss,不作为升级门控主张的支持。

## 5. 实验

**设置**:四基准(Steam/ML1M/Beauty/ATG)从原始数据按冻结协议重建 **[待填:数据集统计表]**;指标 HR@10/NDCG@10,生产选择器;宿主基线=冻结 PreferGrow 跑集。外部 baseline:**[待填:DiffuRec 或 RecFormer 单模型,CLOSE-04;若算力窗口关闭则显式报告缺失]**。超参 τ0=0.2、g_max=0.5、k=2.0、φ(0.70,0.10) 冻结。

**5.1 冻结门四数据集验证(表 2,真实数字,test NDCG@10,p2 选择器,单种子)**:

| 数据集(φ) | 宿主 | 本文 | Δ | 对照预注册预测 |
|---|---|---|---|---|
| Steam(1.00) | 0.0129 | 0.0149 | +0.0020 | 方向 ✓,量级 ×(参考 ≥+0.003) |
| ATG(0.12) | 0.0419 | 0.0305 | −0.0114 | 平价 × |
| Beauty(0.00) | 0.0333 | 0.0294 | −0.0039 | 平价 ✓ |
| ML1M(0.00) | 0.0910 | 0.0759 | −0.0151 | 平价 × |

**判读(混合结果,如实报告)**:唯一开门的 Steam 保持正号(+0.0020)但未达 +0.003 参考量级;Beauty 守住平价带;ML1M(−0.0151)与 ATG(−0.0114)分别以 0.0051、0.0014 超出平价带。由于 ML1M/Beauty 在 g≡0 下运行——按命题 2,训练系统**在数学上就是宿主核**——关门数据集的这种偏差度量的是协议噪声(run-to-run 种子方差与最优步选择:宿主选中 30.2 万步,验证跑选中 40.5 万步),而非门的性质。我们因此把 ML1M/ATG 未中作为**待查实现红灯**处理 **[待填:CLOSE-02 宿主噪声地板(2-3 个 core 种子);据其把红灯归因为噪声内或实现问题]**,并**暂停**"精确归约已被端到端经验演示"的表述。理论命题不受影响;上线前的离线数值等价套件(相同 p1 下腐蚀行/score-entropy 项/反演比率逐项匹配)已通过。
*(注释中保留 GATE-2 升级句:若 CLOSE-02 显示偏差在宿主噪声地板内,升级为"关门跑与宿主统计不可区分,正如归约定理所预测"。)*

**5.2 对照与机制证据(第一代实例化,Beauty token-dropout 语境)**:表 3 冻结数字——aligned 门控 0.0393 / 无门锚点 0.0366 / u-shuffle 0.0206 / global-p 0.0289(test NDCG@10)。三个结论:对齐门控是承重的(shuffle 崩到 0.0206);通用偏置解释不了(global-p 仅 0.0289);门自适应回退(描述置空腐蚀下可靠性均值 −0.0134 且回退质量对应上升;token dropout 同向 −0.0031;门控变体从干净到腐蚀退化小 5 倍:−0.0008 vs −0.0043)。**[待填:冻结两因子门下的 v2 对照臂——Beauty 四臂已在 clean root 完成、Steam 在跑(CLOSE-01);从 sprint07_control_table.csv 回填并把表述限定在已完成臂]** **[图待生成:回退机制图]**

**5.3 注入位置分析(限定表述)**:相同信号注入三个位置(encoder/loss/kernel)。第一代跑中,较早注入位置在干净 regime 有竞争力,核侧条件化在腐蚀下最强;**作为限定于该实例化的 regime 观察报告,不主张核位置一贯获胜**。按注记,核位置的论点不是全 regime 取胜,而是唯一能表达 fallback 保证的位置。*(v2 三方重跑已于 2026-07-06 剪枝取消;不做跨位置优越性主张。)*

## 6. 从门到调度(新增讨论节,零经验主张)

我们的门沿腐蚀轨迹是**常数**乘子:同一个 g 作用于每个噪声档。上述验证结果——开门数据集保号但未达参考量级、ASO 纯 out-of-sample 未中——与"常数门是过钝的执行器"相容:把腐蚀向内容倾斜的价值,在轻腐蚀(褪色物品几乎可恢复)与重腐蚀(proposal 支配状态分布)之间很可能不同。一个简单的代数事实使时变扩展是良定义的:形如 αI+(1−α)Π_p 的转移在 proposal **各不相同**时的乘积仍保持同一形式,有效 proposal 是路径上各步 proposal 的显式凸组合。因此闭式边缘、score-entropy 权重与归约性质在调度依赖的门控下**幸存**,而效用统计量 U_ds 自然成为**腐蚀调度**而非标量门的定价。以与本文相同的预注册纪律发展这一证据定价调度族,是本工作的直接延续。

## 7. 相关工作(五轴)

①**扩散推荐器**(连续:DreamRec/DiffuRec/PreferDiff;离散:PreferGrow/LLaDA-Rec;我们补宿主的核侧一半);②**[新增]离散扩散 LM 与生成式推荐的自适应腐蚀**(LLaDA/SEDD 底座;LLaDA-Rec 并行 SID、DiffGRM 置信度任意序解码、2601.19501 位置自适应掩码、2511.23021 分析——它们自适应于模型内生信号,无外部证据定价、无误导信号下的归约保证;本文是互补且警示性的:第一份预注册证据表明证据塑造腐蚀的价值会随证据质量**倒置**,并给出训练前定价统计量与可证安全的条件化构造——我们视之为自适应腐蚀推荐器将需要的**可靠性层**);③**塑形转移核**(D3PM/CDRec/TDPM:全局或物品级信号,无按用户证据条件化、无归约保证、无效用逆序报告);④**Guidance 与自适应**(CFG 及其推荐应用、语言扩散的置信度自适应 guidance:score 侧全局权重;我们是核侧类比,权重由可测的 history-only 证据统计量设定,插值目标是宿主可学习 proposal——这正是安全性可证的原因);⑤**推荐中的可靠性与侧信息 / 负采样中的假负例**(UGR/TruthSR、RecFormer/UniSRec;对比学习与隐式 CF 中的 FN 文献:我们把该失败模式搬运到扩散**核**——"负采样器"就是转移律本身,与平稳分布、细致平衡、训练目标纠缠——并提供训练前定价统计量 U_ds)。

## 8. 结论与限制

(结论段:冻结措辞——核侧两因子 history-only 证据门、fallback-safe 三件套、逆序发现的独立价值、"有界下行的赌注"框架、U_ds 训练前定价。)

**限制(如实)**:跨数据集逆序仅四点(p=1/24),用户级不复现(ρ=0.028/0.046,弱且略正)——逆序保持数据集级观察,非用户级机制律;冻结门验证混合——开门保号未达参考量级、ASO out-of-sample 直接未中、两个关门平价检查以待归因的差距未中 **[待填:CLOSE-02 噪声地板]**——在此之前**不主张归约定理已被端到端演示**;关门只能与宿主持平——高效用数据集上方法价值是安全而非增益;机制证据来自参数化不同的第一代实例化;φ 铰链粗糙(尽管预注册且 65% 平台稳健);ML1M 单长度桶;保证限于平稳 proposal 核——掩码/调度型离散扩散需要自己的分析(见第 6 节);metadata-sparsity 假设未获支持,作为负结果入附录。

## 附录(骨架,CLOSE-07 从冻结稿合并全文)

A 证明(秩一代数/闭式/细致平衡/反演/碰撞恒等式)| B 范围与边界条件(history-only、自重采样腐蚀、伪物品坐标)| C U_ds 估计量与预注册(4000 转移/seed 7/100 流行度负例;(0.70,0.10) 先冻结后计算)| D 第一代实例化(机制证据与失败模式记录,非最终系统基准)| E 实现与基建(冻结超参/选择器/manifest 哈希链;可复现性材料随补充材料提交,checklist 单独上传)| F 负结果(metadata-sparsity)

---

### 与英文版的占位符对账(提交前必须清零)

| 占位符 | 责任行 | 期限 |
|---|---|---|
| 数据集统计表 | CLOSE-07 | 7/27 |
| 外部 baseline 表或显式缺失声明 | CLOSE-04 | 7/16 |
| 宿主噪声地板+红灯归因措辞 | CLOSE-02 | 7/13 |
| v2 对照臂表(SPRINT-07) | CLOSE-01 | 7/12 |
| 回退机制图 | CLOSE-07 | 7/27 |
| 附录全文合并(A/B/D/F)+ bib 元数据核实 | CLOSE-07 | 7/27 |
| Gate-2 定稿措辞(升级句启用与否) | CLOSE-05 | 7/14 |
