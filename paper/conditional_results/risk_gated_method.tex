% Include only when the immutable RISK-08 exit is risk_gated_method.
The preregistered atomic seed-100 pilot completed its full 14-task
matrix and selected the \texttt{risk\_gated\_method} exit. We report the
result as a single-run observation under the frozen evaluator and
validation selector. The release supports the preregistered pilot
criterion; it does not establish statistical significance, seed
stability, or a population-level performance guarantee.
