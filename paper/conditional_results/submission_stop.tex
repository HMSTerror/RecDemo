% Include only when the immutable RISK-08 exit is submission_stop.
The preregistered atomic seed-100 pilot selected the
\texttt{submission\_stop} exit. In accordance with the frozen no-rescue
contract, we do not tune the threshold, change the corruption ladder,
add a favorable subset, or launch another seed to reverse the outcome.
The remaining claims are limited to the reliability audit, scoped
kernel theory, and preserved negative evidence.
