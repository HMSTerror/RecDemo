% Include only when the immutable RISK-08 exit is audit_only.
The preregistered atomic seed-100 pilot selected the
\texttt{audit\_only} exit. We therefore retain the exposure-risk
measurement, scoped implementation trace, and exact kernel fallback as
audit evidence, while withholding a gate-efficacy conclusion. No
threshold, corruption level, selector, or additional seed was introduced
after observing the exit.
