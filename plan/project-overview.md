# PreferGrow AAAI-27 paper closeout

The active objective is to close the evidence and wording gaps for the AAAI-27 submission without altering the frozen Table 2 values or overstating single-run observations. The authoritative execution state is `issues/2026-07-06_evidence-priced-schedule-and-closeout.csv`.

The current Gate-2 revision must keep theory and empirical evidence separate. Exact reduction is a kernel-level statement at `g=0`; nonzero-gate total-variation bounds apply only to the proposal and one-step forward transition rows. E1 has produced a step-0 optimizer-ownership hard stop, so the paper cannot claim an end-to-end production-path demonstration of exact reduction. CLOSE-10 supplies only a provenance-limited observed three-run ATG spread because all three frozen run manifests are absent.
