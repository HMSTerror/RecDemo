# PreferGrow AAAI-27 paper closeout

## 2026-07-13 local fixed-disk objective

The active local objective is to turn the already archived evidence into a submission-ready,
hash-traceable evidence layer while r7 continues on the server. This work does not authorize new
training, change r7, create RISK-08 evidence, tune a gate from validation/test metrics, or fill a
missing result with a planning value.

The current deliverables are: an EPE/`phi_R` method amendment; a legacy-versus-corrected evaluator
arbitration; an E5 SASRec implementation/data-contract audit; a fail-closed r7 result ingestion
builder; a bilingual Gate-2 amendment; claim--evidence traceability; figure/table data contracts;
and an operational handoff. The final scientific exit remains controlled by the original r7
RISK-08 artifact.

The active objective is to close the evidence and wording gaps for the AAAI-27 submission without altering the frozen Table 2 values or overstating single-run observations. The authoritative execution state is `issues/2026-07-06_evidence-priced-schedule-and-closeout.csv`.

The historical Gate-2 paragraph below is retained as provenance, but its E1 state is superseded by
the dated R12 trace evidence. Any new amendment must state both the original hard stop and the
scope-limited repair outcome rather than silently deleting either event. Exact reduction remains a
kernel-level statement at `g=0`; nonzero-gate total-variation bounds apply only to the proposal and
one-step forward transition rows.
