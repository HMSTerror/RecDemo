# r7 method-scope integration outline

1. Revise the English abstract, synchronized Chinese overview, and standalone abstract-freeze candidate around a generalized frozen dataset scale `s_D` rather than presenting every result as the original `phi(U_ds)` gate.
2. Separate legacy `U_ds` discovery, RISK-03 EPE/PNE@10 exposure measurement, and the dated r6a/r7 `phi_R` intervention. Disclose that the frozen `phi_R` values are increasing in observed EPE and are therefore interpreted as evidence retention/corruption reliability, not demonstrated high-risk suppression.
3. Keep all fallback and total-variation claims at kernel scope: exact reduction at `g=0`, proposal TV, and one-step transition-row TV. R12 is revision/path evidence, not checkpoint or metric equivalence.
4. Qualify the four-domain inversion as a legacy-scale descriptive ordering. Report that corrected-evaluator arbitration weakens strict rank agreement to 3/4 through an adjacent Beauty/ATG swap; keep `1/24` descriptive only.
5. Preserve the frozen Table 2 cells but label their legacy selector/evaluator scope. State one closed-gate ML1M miss and one barely-open ATG (`phi=0.117`) miss without noise-equivalence language.
6. Add the r6a controlled-corruption precursor table with validation and test side by side. Identify Steam c60 as a same-direction seed-100 observation, Beauty c0/c60 as test-only observations, and c100 as explicit `phi_R=0` best-summary recovery rather than `u_tilde` collapse.
7. Replace DiffuRec as the confirmatory external baseline with the completed four-domain SASRec atomic result; disclose the unresolved Beauty validation-to-test anomaly. Retain DiffuRec only in related work and evaluator-audit provenance.
8. State E7 as `not_estimable` with zero executed bootstrap replicates, retain validation-only model selection and development-time test logging, and label every seed-100 result a single-run observation.
9. Add mechanical Go/No-Go rules: 7/16 r7-start/RISK-08 executability, 7/18 no-anchor efficacy downgrade, and unconditional no-rescue after `submission_stop`.
10. Compile or perform an honest structural fallback validation, then synchronize line-level evidence coverage and capability-use audit.
