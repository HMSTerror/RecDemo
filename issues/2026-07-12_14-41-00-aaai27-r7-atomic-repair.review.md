# r7 atomic repair vision review log

## REVIEW-01

- Source doc: `docs/superpowers/specs/2026-07-12-aaai27-r7-atomic-repair-design.md`
- Review agent: fallback independent-context
- Validation limitation: `validation_limited:same-model sub-agent unavailable after two operational timeouts`; `/root/review_r7` and `/root/review_r7_retry` were each launched with no inherited context, instructed to work read-only, and interrupted only after repeated no-output timeouts. This fallback does not claim that a same-model sub-agent review completed.
- Scope checked: P0-0 through P0-6; git range `48db0b9..22335c0`; anchor override and final proposal; full-c100 fallback; wrapper/log/artifact contract; artifact-derived RISK-08; r7 resident scheduling/finalization; EPE/`phi_R`/Beauty/SASRec/E7/test-use wording; source/queue hashes and live external-GPU limitation.
- Evidence checked: approved design and CSV; commits `bdc8422` through `22335c0`; `model/text_side.py`; pilot adapter/wrapper/report/finalizer/resident code; dedicated tests and dated reports; P0-6 hash/compile package; fresh 135-module regression (`135 tests`, `0 failures`, `1` existing skip); fresh live status showing `14 ready`, `0 running`, `0 records`, `0 GPU-h`, no RISK-08 exit.
- Claim/evidence alignment: mismatches found.
- Limited validation honestly reported: yes. P0-5 is explicitly `waiting_external_gpu`, not training completion. P0-4 dry-run/smoke and P0-2/P0-3 fixtures are labeled contract evidence, not production performance. P0-6 is fallback article-mode compilation, not official AAAI-style validation.
- Result: gaps_found
- Strengths:
  - `scripts/aaai27_adapters/pilot_adapters.py:213-227` binds exactly one anchor override `1.0`; `tests/test_r6_launch_contract.py:174-227` exercises the production builder and checks final anchor proposal differs from core, equals the anchor proposal, and uses `g=g_max`.
  - `scripts/aaai27_adapters/pilot_task_wrapper.py:280-345` creates a wrapper audit log before the child, fsyncs it, rejects empty logs before emitting a manifest, and binds summary/log hashes. `scripts/aaai27_adapters/risk04_08.py:708-781` revalidates those bytes; the empty-log tamper test passes.
  - `scripts/aaai27_adapters/risk04_08.py:820-881` rejects hand-entered metrics, requires the complete frozen branch, resolves every artifact under the same queue root, and revalidates every task manifest. `pilot_report.py` derives the report from those artifacts.
  - `scripts/aaai27_queue/runtime.py:263-289` uses the manifest GPU allowlist, local-process exclusion, queue-root locks, and physical compute-PID probes. `scripts/aaai27_r7_resident_queue.py:325-403` fails on inactive records, writes immutable task-failure/stop terminals, and finalizes only after all 14 active tasks pass.
  - Paper wording correctly separates legacy `U_ds`, EPE/PNE@10, and controlled `phi_R`; discloses the increasing-in-EPE sign; co-locates Beauty validation/test; identifies c100 as explicit scale-zero; excludes DiffuRec from confirmatory comparison; cites SASRec; and reports E7/test-use limitations.
- Gaps:
  1. Important — `docs/reports/data/2026-07-12-r7-anchor-adapter-repair/anchor_gate_source_test_report.md:87` marks “Full c100 remains exact `p_core`” as passed using “closed-gate gradient/identity tests plus unchanged production file.” The only scale-zero test at `tests/test_text_side_proposal.py:180-210` checks gradient identity, not direct value equality, while P0-1 acceptance explicitly requires a full-c100 run returning `p_core` exactly. Production code at `model/text_side.py:599-602` appears correct, so this is a regression-evidence gap, not a demonstrated runtime bug. Fix: add a direct manifest-bound Beauty/Steam full-c100 production-builder test asserting exactly one `0.0` override, `g==0`, and `proposal` byte/tensor equality with `p_core`; mutation-prove the test and rerun the full 135-test set.
  2. Minor — P0-6 CSV refs name `docs/runbooks/...manual.md:307`, but the dated amendment begins at line 309. Fix the stale reference while closing the follow-up.
- Follow-up issues added: `FOLLOWUP-01`, then `REVIEW-02`.
- Assumptions: production `model/text_side.py` remains unchanged; no r7 result exists yet; live remote artifacts remain authoritative over documents.
- Decision debt: official AAAI author-kit validation and three real figures remain outside this repair review; P0-5 must receive a separate terminal review after actual GPU execution.
- Human-required blockers: none for the evidence-test follow-up. GPU release remains an external-state dependency for P0-5.

## REVIEW-02

- Source doc: `docs/superpowers/specs/2026-07-12-aaai27-r7-atomic-repair-design.md`
- Review agent: same-model sub-agent (`/root/review_r7_followup`), independent read-only scope. The reviewer returned substantive findings through the mailbox and was interrupted only after it remained live without emitting a second final-status envelope.
- Scope checked: FOLLOWUP-01 and git range `9d9f0a8..953fd0d`; direct Beauty/Steam full-c100 manifest-to-builder evidence; mutation sensitivity; local/server source identity; dated report and CSV provenance; retained 12-module regression; all seven REVIEW-01 claim boundaries; live P0-5 external-GPU state.
- Evidence checked: approved design, CSV, REVIEW-01 log, exact diff, `tests/test_r6_launch_contract.py:251-295`, `model/text_side.py:598-602`, `scripts/aaai27_adapters/pilot_adapters.py:244-262`, dated anchor report, fresh targeted test (`1/1`), fresh 12-module regression (`136 tests`, `0 failures`, `1` existing skip), reviewer in-memory mutation failure on Beauty full-c100, local SHA-256 `ae28c3b2...05c46a`, identical Git blob `6fa1fae...` at r7 source revision/HEAD, and live remote controller/GPU status.
- Claim/evidence alignment: core mismatch closed; two minor documentation/provenance mismatches found.
- Limited validation honestly reported: yes. The direct regression proves the generated r7 manifest-to-production-builder contract, not completion of any GPU training task. P0-5 remains `waiting_external_gpu`; live status was `14 ready`, `8 inactive pending`, `0 running`, `0 records`, `0 GPU-h`, and no RISK-08 exit while both L20s remained occupied by root compute PIDs `2987761` and `3072363`.
- Result: gaps_found (Minor only).
- Strengths:
  - The new test selects exactly the Beauty and Steam full-c100 tasks, requires exactly one explicit scale `0.0` plus `ablation_mode=none`, instantiates the production builder, and checks both exact-zero `g` and tensor-exact `proposal == p_core`.
  - The reviewer independently removed the scale-zero closed-gate condition in memory; the same test failed at the value assertion for Beauty, establishing mutation sensitivity without altering the working tree.
  - Fresh targeted and 12-module regressions passed, and `model/text_side.py` is unchanged across the reviewed range and the frozen r7 Git source revision.
- Gaps:
  1. Minor — `docs/reports/data/2026-07-12-r7-anchor-adapter-repair/anchor_gate_source_test_report.md:105` still says the server has not received the patch and production deployment remains P0-4, although P0-2/P0-3/P0-4 are complete and the P0-5 resident controller is already armed. Replace this with a time-accurate boundary: FOLLOWUP-01 is test/report-only, the frozen r7 server source is intentionally unchanged, no redeploy is required, and P0-5 still waits for GPU execution.
  2. Minor — P0-1 correctly preserves the original report hash `07c4...` as historical evidence, but FOLLOWUP-01 does not bind the amended report hash. Add the current SHA-256 `4c949d34af18b9bda4b2196d461e9850263dc8d52cd6f1825e1822d942b4d4bf` to FOLLOWUP-01 notes and explicitly label the P0-1 value as the pre-follow-up hash.
- Follow-up issues added: `FOLLOWUP-02`, then `REVIEW-03`.
- Assumptions: line-ending-dependent local/server byte hashes are not source divergence; the normalized Git blob `6fa1fae12e4655b65ba301241b5ae03573e5d27f` is identical at revision `987eb19` and HEAD.
- Decision debt: official AAAI author-kit validation, three real figures, and actual r7 performance/RISK-08 results remain outside this implemented/paper-scope review.
- Human-required blockers: none for FOLLOWUP-02. GPU release remains an external-state dependency for P0-5.

## REVIEW-03

- Source doc: `docs/superpowers/specs/2026-07-12-aaai27-r7-atomic-repair-design.md`
- Review agent: fallback independent-context. Same-model sub-agent `/root/review_r7_provenance_final` was launched with no inherited context and a read-only, narrowly scoped prompt, but returned no findings after repeated waits and an explicit request to finalize; it was interrupted. This is recorded as `validation_limited:same-model sub-agent operational timeout`, not as a completed sub-agent review.
- Scope checked: FOLLOWUP-02 and git range `ea9744d..a1b6a79`; corrected deployment wording; amended/historical report hashes; production diff and normalized Git blob; direct full-c100 regression; scientific-claim immutability; live P0-5 controller/GPU/artifact state.
- Evidence checked: exact FOLLOWUP-02 diff; final report SHA-256 `b7bce3fb40d2bda515b2f902d49257646746f5382c7ce07122154d20264ae3e6`; normalized `model/text_side.py` Git blob `6fa1fae12e4655b65ba301241b5ae03573e5d27f` at frozen r7 revision `987eb19` and HEAD; empty FOLLOWUP-02 production-file diff; fresh direct c100 test (`1/1`, `OK`); fresh remote controller JSON and GPU compute-PID query.
- Claim/evidence alignment: matched for the implemented and paper scope.
- Limited validation honestly reported: yes. The same-model reviewer did not return a report. No r7 training task has started or completed: the live controller remains healthy with `14 ready`, `8 inactive pending`, `0 running`, `0 records`, `0 GPU-h`, `stop_requested=false`, and no RISK-08 exit while both L20s remain occupied by root PIDs `2987761` and `3072363`.
- Result: vision_met (implemented/paper scope only).
- Strengths:
  - The stale deployment sentence is replaced by a precise boundary: FOLLOWUP-01 is local test/report-only, the frozen server source stays at `987eb19` by design, no redeploy is required, and P0-5 remains external-GPU-limited.
  - FOLLOWUP-01 binds the final amended report hash while explicitly preserving P0-1's `07c4...` hash as the historical pre-follow-up version.
  - FOLLOWUP-02 changed no model, adapter, wrapper, runtime, controller, metric, paper claim, or remote queue state.
  - The direct full-c100 contract still passes and the production source blob remains identical to the frozen r7 source revision.
- Gaps: none in implemented/paper scope.
- Follow-up issues added: none.
- Assumptions: remote artifacts override documents; any later r7 result must be integrated only after the immutable RISK-08 exit exists.
- Decision debt: P0-5 requires a separate terminal review after actual GPU execution; official AAAI style/page validation and the three real figures remain outside this r7 repair review.
- Human-required blockers: external root-owned GPU occupancy is the only remaining blocker for P0-5 execution; no user action is required for the detached controller to continue waiting.
